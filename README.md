# Instalación

`apt update -y && apt upgrade -y`

`pkg update -y && pkg upgrade -y`

`pkg install git -y && pkg install python`

`pip install colorama && pip install requests`

`git clone https://github.com/ProxyCKL/DOX-NUMBER`

`cd DOX-NUMBER`

`python DOX-NUMBER.py`
